<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:loc="clr-namespace:Chummer.NewUI.Markup"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Chummer.NewUI.Controls"
             xmlns:vm="clr-namespace:Chummer.NewUI.ViewModels"
             x:Class="Chummer.NewUI.Controls.CharacterSections.CyberwareSectionTab"
             x:DataType="vm:CyberwareSectionViewModel">
    <controls:TreeDetailPane ContentMargin="4">
        <controls:TreeDetailPane.HeaderContent>
            <WrapPanel Margin="0,4,0,0">
                <Button Content="{loc:Loc Button_AddCyberware}" Click="OnAddCyberwareClick" Margin="0,0,4,4" />
                <Button Content="{loc:Loc Button_AddBioware}" Click="OnAddBiowareClick" Margin="0,0,4,4" />
            </WrapPanel>
        </controls:TreeDetailPane.HeaderContent>
        <controls:TreeDetailPane.TreeContent>
            <TreeView Name="CyberwareTree" ItemsSource="{Binding Roots}" SelectedItem="{Binding SelectedNode, Mode=TwoWay}">
                <TreeView.Styles>
                    <Style Selector="TreeViewItem">
                        <Setter Property="IsExpanded" Value="True" />
                    </Style>
                </TreeView.Styles>
                <TreeView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="{loc:Loc UI_CyberwareSuite}" Click="OnAddCyberwareSuiteClick" />
                        <MenuItem Header="{loc:Loc UI_BiowareSuite}" Click="OnAddBiowareSuiteClick"
                                  IsVisible="{Binding CanAddBiowareSuites}" />
                        <Separator />
                        <MenuItem Header="{loc:Loc Tab_Notes}" Click="OnEditNotesClick" />
                        <MenuItem Header="{loc:Loc String_Delete}" Click="OnDeleteClick" />
                        <MenuItem Header="{loc:Loc UI_Sell}" Click="OnSellClick" />
                        <MenuItem Header="{loc:Loc Menu_EditCopy}" Click="OnCopyClick" />
                        <MenuItem Header="{loc:Loc Menu_EditPaste}" Click="OnPasteClick" />
                    </ContextMenu>
                </TreeView.ContextMenu>
                <TreeView.ItemTemplate>
                    <TreeDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}" />
                    </TreeDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </controls:TreeDetailPane.TreeContent>
        <controls:TreeDetailPane.DetailContent>
            <StackPanel Margin="8,0,0,0" Spacing="6">
            <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="4">
                <TextBlock Grid.Row="0" Grid.Column="0" Text="{loc:Loc Label_Name}" Width="80" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedNode.Name, TargetNullValue=–}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="{loc:Loc Label_Category}" Width="80" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedNode.Category, TargetNullValue=–}" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="{loc:Loc UI_RatingColon}" Width="80" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedNode.Rating, TargetNullValue=–}" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="{loc:Loc Checkbox_Transgenic}" Width="80"
                           IsVisible="{Binding SelectedNode.IsTransgenic}" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="✓" Foreground="#287A3D"
                           IsVisible="{Binding SelectedNode.IsTransgenic}" />
            </Grid>
            <Grid ColumnDefinitions="Auto,*">
                <TextBlock Text="{loc:Loc UI_CostAvailShortColon}" Width="80" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                    <TextBlock Text="{Binding SelectedNode.Cost, TargetNullValue=–}" />
                    <TextBlock Text="¥ /" />
                    <TextBlock Text="{Binding SelectedNode.Avail, TargetNullValue=–}" />
                </StackPanel>
            </Grid>
            <Separator Margin="0,4" />
            <TextBlock Text="{loc:Loc Label_EssenceConsumption}" FontWeight="Bold" />
            <Grid ColumnDefinitions="Auto,60" RowDefinitions="Auto,Auto" RowSpacing="2">
                <TextBlock Grid.Row="0" Grid.Column="0" Text="{loc:Loc Label_Cyberware}" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CyberwareEssence}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="{loc:Loc Label_Bioware}" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding BiowareEssence}" />
            </Grid>
            </StackPanel>
        </controls:TreeDetailPane.DetailContent>
    </controls:TreeDetailPane>
</UserControl>
