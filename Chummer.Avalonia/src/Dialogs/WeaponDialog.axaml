<Window xmlns="https://github.com/avaloniaui"
             xmlns:loc="clr-namespace:Chummer.NewUI.Markup"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Chummer.NewUI.ViewModels"
        xmlns:controls="clr-namespace:Chummer.NewUI.Controls"
        x:Class="Chummer.NewUI.Dialogs.WeaponDialog"
        x:DataType="vm:WeaponDialogViewModel"
        Width="640" Height="520" WindowStartupLocation="CenterOwner" Title="{loc:Loc Title_SelectWeapon}">
    <Grid RowDefinitions="Auto,*,Auto" Margin="12">
        <Grid Grid.Row="0" ColumnDefinitions="Auto,150,Auto,*" Margin="0,0,0,8">
            <TextBlock Grid.Column="0" Text="{loc:Loc Label_Category}" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" />
            <TextBlock Grid.Column="2" Text="{loc:Loc Label_Search}" VerticalAlignment="Center" Margin="16,0,6,0" />
            <TextBox Grid.Column="3" Text="{Binding SearchText, Mode=TwoWay}" />
        </Grid>
        <Grid Grid.Row="1" ColumnDefinitions="*,220">
            <ListBox ItemsSource="{Binding WeaponOptions}" SelectedItem="{Binding SelectedWeapon, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:WeaponOptionViewModel">
                        <StackPanel Margin="4,2">
                            <TextBlock Text="{Binding Name}" Foreground="#1E5FA8" />
                            <TextBlock Text="{Binding Category}" FontSize="10" Foreground="#666666" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Grid.Column="1" Margin="12,0,0,0" Spacing="6">
                <TextBlock Text="{loc:Loc Label_Category}" FontWeight="Bold" />
                <TextBlock Text="{Binding SelectedWeapon.Category, TargetNullValue=–}" />
                <TextBlock Text="{loc:Loc Label_Damage}" FontWeight="Bold" />
                <TextBlock Text="{Binding SelectedWeapon.Damage, TargetNullValue=–}" />
                <TextBlock Text="{loc:Loc UI_ArmorPenetrationColon}" FontWeight="Bold" />
                <TextBlock Text="{Binding SelectedWeapon.Ap, TargetNullValue=–}" />
                <TextBlock Text="{loc:Loc Label_Mode}" FontWeight="Bold" />
                <TextBlock Text="{Binding SelectedWeapon.Mode, TargetNullValue=–}" />
                <TextBlock Text="{loc:Loc UI_AmmoColon}" FontWeight="Bold" />
                <TextBlock Text="{Binding SelectedWeapon.Ammo, TargetNullValue=–}" />
                <TextBlock Text="{loc:Loc Label_Avail}" FontWeight="Bold" />
                <TextBlock Text="{Binding SelectedWeapon.Availability, TargetNullValue=–}" />
                <TextBlock Text="{loc:Loc Label_Cost}" FontWeight="Bold" />
                <TextBlock Text="{Binding SelectedWeapon.Cost, TargetNullValue=–}" />
                <TextBlock Text="{loc:Loc Label_Source}" FontWeight="Bold" Margin="0,12,0,0" />
                <controls:SourceLink SourcePage="{Binding SelectedWeapon.SourcePage}" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2" ColumnDefinitions="*,Auto,Auto" Margin="0,12,0,0">
            <Button Grid.Column="1" Content="{loc:Loc String_Cancel}" Width="80" Margin="0,0,6,0" Click="OnCancel" />
            <Button Grid.Column="2" Content="{loc:Loc UI_Add}" Width="80" Click="OnOk" />
        </Grid>
    </Grid>
</Window>
